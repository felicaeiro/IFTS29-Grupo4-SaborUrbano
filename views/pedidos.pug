doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
        h1 Pedidos
        if pedidos.lenght
            ul
                each pedido in pedidos
                    li 
                        p pedido.fecha
                        p pedido.total
        